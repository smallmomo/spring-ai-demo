spring:
  application:
    name: my-mcp-client
  ai:
    openai:
      base-url: https://open.bigmodel.cn/api/paas/v4
      api-key: ${ZHIPU_API_KEY}     # 建议放在环境变量中
      chat:
        completions-path: /chat/completions
        options:
          model: glm-4.5          # 或者 glm-4-5、glm-4-flash 等
    mcp:
      client:
        sse:
          connections:
            server1:
              url: http://localhost:8081
#            server2:
#              url: https://mcp.amap.com
#              sse-endpoint: /sse?key=************
server:
  port: 8082